version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    network_mode: "host"  # Required for Sonos speaker discovery on local network
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
